[project]
name = "package-manager-helper"
version = "0.1.0"
description = "Helper project for generating uv template files"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    # :TODO: Remove pylint when ruff supports all pylint rules
    # :TODO: Remove black when ruff supports all black rules
]
