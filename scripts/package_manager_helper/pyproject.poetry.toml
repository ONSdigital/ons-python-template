[project]
name = "package-manager-helper"
version = "0.1.0"
description = "Helper project for generating template files"
readme = "README.md"
license = "MIT"
authors = [{ name = "Template Helper" }]
requires-python = ">=3.11"
dynamic = ["dependencies"]

[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
# :TODO: Remove pylint when ruff supports all pylint rules
# :TODO: Remove black when ruff supports all black rules
